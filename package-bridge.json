{
  "name": "freeplane-integration-bridge",
  "version": "1.0.0",
  "description": "HTTP bridge for Freeplane MCP Server and n8n integration",
  "type": "module",
  "main": "mcp-http-bridge.js",
  "scripts": {
    "start": "node mcp-http-bridge.js",
    "dev": "node --watch mcp-http-bridge.js",
    "setup": "cd freeplane-mcp-server && npm install && npm run build && cd ..",
    "test-bridge": "curl http://localhost:3000/health"
  },
  "keywords": [
    "freeplane",
    "mcp",
    "n8n",
    "automation",
    "ai",
    "mindmap"
  ],
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
